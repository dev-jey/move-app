import { CustomTitlecasePipe } from './custom-titlecase.pipe';

describe('CustomTitlecasePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomTitlecasePipe();
    expect(pipe).toBeTruthy();
  });
});
